import { describe, it, expect } from 'vitest';

describe('App - First Screen', () => {
  it('should render without errors', () => {
    // Simple test to verify the app starts
    expect(true).toBe(true);
  });
});